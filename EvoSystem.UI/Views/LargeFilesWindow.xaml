<Window x:Class="EvoSystem.UI.Views.LargeFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Arquivos Grandes Encontrados" Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Border Background="#1a1c2a" CornerRadius="10" BorderThickness="1" BorderBrush="#33ffffff" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="TitleText" Text="Arquivos Grandes (> 100MB)" FontSize="20" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="TxtStatus" Text="Varrendo pastas do usuário..." Foreground="#007bff" Margin="0,5,0,15"/>
            </StackPanel>

            <DataGrid x:Name="GridFiles" Grid.Row="1" AutoGenerateColumns="False" 
                      Background="#1a1c2a" RowBackground="#1a1c2a" Foreground="White"
                      BorderThickness="0" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      CanUserAddRows="False">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#25283d"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderBrush" Value="#33ffffff"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="250" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Caminho Completo" Binding="{Binding FullName}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Tamanho (Bytes)" Binding="{Binding Length}" Width="120" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button x:Name="BtnClose" Grid.Row="2" Content="Fechar" Width="100" Height="35" HorizontalAlignment="Right" Margin="0,20,0,0" Click="BtnFechar_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#ff4d4d"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>